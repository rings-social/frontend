export type Settings = {
    // The name of the ring to display on the home page
    homeRing: string,
    
    // The base URL of the site
    baseUrl: string,

    // Auth0 Settings
    auth0Domain: string,
    auth0ClientId: string,
};